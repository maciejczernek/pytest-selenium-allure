[pytest]
python_files = test_*
python_classes = *Tests
python_functions = test_*

addopts =
    --alluredir ../test_results/allure-results
    --clean-alluredir
    -v
    -s
    -n auto

markers =
    smoke: smoke tests
    main_page: main page tests